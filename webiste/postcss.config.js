module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
    // You might have autoprefixer here already, or Next.js handles it.
    // If you had it from a previous attempt, ensure it's compatible or handled by Next.js.
    // 'autoprefixer': {},
  },
}; 